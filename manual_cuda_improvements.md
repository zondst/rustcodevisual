# Руководство по улучшению и включению CUDA в Particle Studio RS

## 1. Включение поддержки NVIDIA CUDA

Проект уже настроен для использования аппаратного ускорения NVIDIA (NVENC) при экспорте видео.

### Как использовать:
1.  Запустите программу: `target\release\particle_studio_rs.exe`
2.  Перейдите во вкладку **Export** или прокрутите панель настроек вниз.
3.  Найдите раздел **🚀 GPU Acceleration**.
4.  Убедитесь, что отображается название вашей видеокарты (например, `NVIDIA GeForce RTX 3060`).
5.  Поставьте галочку **Use GPU-accelerated export**.
6.  При экспорте видео будет использоваться чип NVENC, что ускорит рендеринг в 3-5 раз по сравнению с процессором.

### Требования:
-   Видеокарта NVIDIA с поддержкой драйверов.
-   Установленный **FFmpeg** в системе (добавлен в `PATH`).

## 2. Улучшения производительности (CPU)

Мы внесли изменения в код для многопоточного обновления частиц.

-   **Parallelize Implementation**: Логика обновления тысяч частиц теперь выполняется параллельно на всех ядрах процессора.
-   **Optimization**: Это позволяет плавно отображать до 20,000+ частиц даже без использования видеокарты для отрисовки интерфейса.

## 3. Советы по настройке

-   Для **максимального качества**: используйте пресет **Cinematic** и увеличьте `Connect Dist` (если доступно) или включите `Volumetric Rendering`.
-   Для **быстрого экспорта**: отключите `Volumetric Rendering` и уменьшите разрешение до 1080p, если 4K занимает слишком много времени.
-   **Режим "Chaos"** отлично реагирует на агрессивную музыку (DnB, Dubstep).
-   **Режим "Calm"** подходит для Ambient/Lo-Fi.

## 4. Сборка проекта

Если вы хотите собрать обновленную версию самостоятельно:

```powershell
cargo build --release
```

Файл будет находиться в `target/release/particle_studio_rs.exe`.
